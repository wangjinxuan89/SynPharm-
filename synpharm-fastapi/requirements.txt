fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
# torch 由 Dockerfile 从 PyTorch CPU 索引单独安装（避免拉取 CUDA 依赖）
numpy==1.26.0
pandas==2.1.0
scikit-learn==1.3.0
python-multipart==0.0.6
# 三个算法真实推理所需依赖：
#   DDI-LLM / KAN-MoDTI 需要 rdkit（分子图 / 摩根指纹）
#   FlashPPI 需要 transformers + einops（trust_remote_code 加载 gLM2 骨架）
rdkit
transformers>=4.40
einops>=0.7
